defmodule ExFunky.Enum do
  @doc """
  Invokes `fun` for each element in the `enumerable` with the `accumulator`.

  ## Examples
  iex> ExFunky.Prelude.id "Hello World!"
  "Hello World!"

  """
  def reduce(enumerable, accumulator, fun) do
    
  end

end